Hypotheses:

log_score(h) = Σ active_cues support(c,h) × utility_strength(c) × confidence_weight(c)
posterior = softmax(log_score)

Hypotheses begin with equal prior odds. Cue activation immediately recomputes scores. Final cue ordering uses utility, active-set discriminativity and confidence; deduplication happens before ranking.
