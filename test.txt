git clone -b feat/pipeline-polish-and-validation ../gg-polish.bundle exp-tst

cp .env.example .env.local
npm ci
npm run setup

Then:

1. Configure the three .env.local values.
2. Import your selected-pool JSON in Choose Pool.
3. Import the .tar.zst precache under Run Precaching into a new data/precache-<name> root.
4. Select that root in setup.
5. Verify it through Demo Experiment.

http://localhost:3000/study?pid=test-participant-001&study=YOUR_STUDY_ID&session=test-session-001
