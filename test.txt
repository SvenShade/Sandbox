AIza

git clone s3d_noise.bundle s3d-noise
  cd s3d-noise/swarm3d_standalone

  python -m venv .venv
  ./.venv/bin/python -m pip install --upgrade pip setuptools wheel
  ./.venv/bin/python -m pip install -e ".[cuda12]"

  PYTHONPATH=src ./.venv/bin/python -m swarm3d.swarm3d_noise_generalisation_ablation \
    --timesteps 200000000 \
    --seeds 0 1 2 \
    --num-envs 256 \
    --eval-episodes 32 \
    --warmup-steps 400000 \
    --preset-buckets tiny,small,medium,large,huge \
    --jax-platform gpu

SyCve8FgG8e4oYKJsFSHQqZSkWNbkcbHQzc
