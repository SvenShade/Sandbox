cd ~/exp-tst
git status --short

git fetch ~/pipeline-vlm-resilience-20260807.bundle \
  feat/pipeline-polish-and-validation:refs/remotes/handoff/vlm-resilience

git switch feat/pipeline-polish-and-validation
git merge --ff-only handoff/vlm-resilience

If the fast-forward fails because the dev branch has diverged, test it separately:

git switch -c test/vlm-resilience handoff/vlm-resilience
